export const publicRoutes = ["/", "/verificar-email"];

export const authRoutes = ["/entrar", "/recuperar-senha", "/nova-senha"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/plataforma";
